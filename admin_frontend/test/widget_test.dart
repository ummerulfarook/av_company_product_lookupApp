import 'package:flutter_test/flutter_test.dart';
import 'package:admin_frontend/main.dart';

void main() {
  testWidgets('Admin app smoke test', (WidgetTester tester) async {
    // Smoke test removed — app requires Provider context to build.
  });
}
